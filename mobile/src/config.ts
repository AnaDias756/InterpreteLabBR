// URL base da API (backend FastAPI).
// Em produção aponta para o backend publicado no Render.
// Para desenvolvimento local com o backend na sua máquina, troque pelo IP da
// sua máquina na rede (ex.: 'http://192.168.0.10:8000') — "localhost" não
// funciona a partir do celular.
export const API_BASE_URL = 'https://interpretador-lab-backend.onrender.com';
